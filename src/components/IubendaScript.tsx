'use client';

// Componente per configurazione aggiuntiva Iubenda (se necessaria)
export const IubendaScript = () => {
  // Lo script principale è già caricato nell'head del layout
  // Questo componente può essere utilizzato per configurazioni aggiuntive se necessarie
  return null;
};

// Componente per il banner cookie (gestito automaticamente da Iubenda)
export const CookieBanner = () => {
  return null; // Iubenda gestisce automaticamente il banner
};
